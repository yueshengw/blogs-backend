{
  "name": "blogs-backend",
  "version": "1.0.0",
  "description": "backend for a blogs application",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand --forceExit",
    "lint": "eslint ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "eslint": "^8.22.0",
    "express": "^4.18.1",
    "http": "^0.0.1-security",
    "mongoose": "^6.5.2",
    "supertest": "^6.2.4"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "nodemon": "^2.0.19"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
